# vismo

**Vismo (`vismo`)** will be the debugging tool that tells you the story behind your live Kubernetes resources. It aims to trace the origin of any configuration field - less looking for particular change, deterministic answer.

MOre VISion -> VISMO